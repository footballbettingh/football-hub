"""The web layer: artifacts on disk, jobs that rebuild them, pages that read them.

`valuebets` asks whether a model can beat the closing line (it cannot).
`confidence` uses the closing line to say how likely an outcome is (it can).
This package is the part you actually look at.
"""

__version__ = "1.0.0"
