# Pinned to the versions the test suite passes on. With open ranges the daily
# run installed whatever was newest that morning — pandas 3 included, the
# first time it was released — and a change nobody made could break the card.
# Dependabot (.github/dependabot.yml) proposes each bump as a pull request, and
# the tests decide it.
numpy==2.2.6
pandas==3.0.6
scipy==1.15.3
requests==2.34.2
python-dotenv==1.2.2
